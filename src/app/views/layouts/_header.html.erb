<!--nav class="navbar navbar-dark bg-dark box-shadow fixed-top navbar-expand-md"-->
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container">
    <div class="navbar-brand d-flex flex-row align-items-center">
      <%= link_to image_tag("WebSiteIcon.png", alt: "icon",width:"50",height:"50"),root_path, class: ""%>
      <strong class="d-none d-lg-block d-md-block"><%= get_the_web_site_name()%></strong>
    </div>
    <div class="navbar-brand col-7">
    <%= form_tag({controller: "youtubeapi_pages", action: "search"},method: :get) do |f| %>
      <div class="input-group">
        <%= text_field_tag "keyword","", {class: 'form-control' , placeholder: "動画を検索"} %>
        <div class="input-group-append">
          <%= submit_tag "検索", class: "btn btn-outline-success" %>
        </div>
      </div>
    <% end %>
    </div> 
    
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav mr-auto"></ul>
      <!--二個目のul属性は右寄せになる-->
      <ul class="navbar-nav">
      <!--ログインしているかどうかでヘッダーの項目を変更できるようにする。-->
        <% if logged_in? %>
          <%= link_to "Users", users_path, class: "btn btn-info mr-sm-4" %>
          <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle mr-sm-4" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Account
            </button>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
              <%= link_to "Profile", current_user, class: "dropdown-item" %>
              <%= link_to "Settings", edit_user_path(current_user), class: "dropdown-item" %>
              <%= link_to "Logout", logout_path,method: :delete ,class: "dropdown-item" %>
            </div>
          </div>
        <% else %>
          <%= link_to "Login", login_path, class: "btn btn-outline-primary mr-sm-4 nav-item" %>
          <%= link_to "Sign up", signup_path, class: "btn btn-outline-info mr-sm-4 nav-item" %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>